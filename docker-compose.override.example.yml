# Example override file for custom configurations
# Copy to docker-compose.override.yml to use

version: '3.8'

services:
  nt8-rl-app:
    # Override environment variables
    environment:
      # Use host network bridge server if running separately
      # - NT8_BRIDGE_HOST=host.docker.internal
      # - NT8_BRIDGE_PORT=8888
      
      # Custom paths
      - MODELS_DIR=/app/models
      - DATA_DIR=/app/data
      
      # GPU support (requires nvidia-docker)
      # - NVIDIA_VISIBLE_DEVICES=all
      # - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    
    # Add GPU support (uncomment if using NVIDIA GPU)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    
    # Add resource limits
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '4'
    #       memory: 8G
    #     reservations:
    #       cpus: '2'
    #       memory: 4G

