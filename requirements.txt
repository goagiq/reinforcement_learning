# NT8 RL Trading Strategy - Python Dependencies

# Core ML/RL Framework
torch>=2.0.0                    # PyTorch (install CUDA version if GPU available)
torchvision>=0.15.0             # Additional PyTorch utilities
gymnasium>=0.29.0              # RL environment interface (successor to OpenAI Gym)
stable-baselines3>=2.0.0       # Pre-built RL algorithms (PPO, DQN, SAC, etc.)
sb3-contrib>=2.0.0             # Additional SB3 algorithms

# Data Processing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0            # Feature normalization, scaling
scipy>=1.11.0                  # Scientific computing (required for Monte Carlo risk assessment)

# Technical Analysis
ta>=0.10.2                     # Technical analysis indicators
pandas-ta>=0.3.14b            # Additional technical analysis

# Communication
requests>=2.31.0               # HTTP requests (Ollama API)
httpx>=0.24.0                  # Async HTTP client (alternative)
websockets>=11.0               # WebSocket support (if needed)

# Monitoring & Logging
tensorboard>=2.13.0           # Training visualization
wandb>=0.15.0                  # Weights & Biases (optional, for experiment tracking)
matplotlib>=3.7.0             # Plotting
seaborn>=0.12.0                # Statistical visualization

# Utilities
python-dotenv>=1.0.0           # Environment variables
pydantic>=2.0.0               # Data validation
pyyaml>=6.0                    # Configuration files
tqdm>=4.65.0                   # Progress bars
watchdog>=3.0.0                # File system monitoring for auto-retrain

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Code Quality
black>=23.7.0                   # Code formatting
flake8>=6.1.0                  # Linting
mypy>=1.5.0                     # Type checking

# Web Framework (for Production UI)
fastapi>=0.104.0                # Fast API framework
uvicorn[standard]>=0.24.0       # ASGI server
websockets>=12.0                # WebSocket support
python-multipart>=0.0.6         # File upload support
psutil>=5.9.0                   # System utilities

# Optional: GPU Support (install separately if using GPU)
# torch>=2.0.0+cu118            # CUDA 11.8 version
# torch>=2.0.0+cu121            # CUDA 12.1 version

# Optional: Advanced Features
# ray>=2.6.0                     # Distributed training (if needed)
# optuna>=3.3.0                  # Hyperparameter optimization

# Agentic Swarm (Multi-Agent System)
strands-agents 
strands-agents-tools
anthropic>=0.40.0                  # Anthropic Claude API client (required by strands.models.anthropic)
ollama>=0.3.0                      # Ollama Python client (required by strands.models.ollama)
